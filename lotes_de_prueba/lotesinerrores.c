int fedad() {
    int edad;
    cout << "Ingresa tu edad: ";
    cin >> edad;
    if (edad >= 18) {
        cout << "Mayor de edad.\n";
    } else {
        cout << "Menor de edad.\n";
    }
    return 0;
}

int fnum() {
    int contador = 1;
    while (contador <= 5) {
        cout << "Número: " << contador;
        contador= contador + 1;
    }
    return 0;
}

char letra;         
char nombre[50];
void err[50];
float decimal = 3.14; 
   
int fsuma(int var456) {
    int num1;
    int num2;
    int i, j, k[20];
    var456();
    num1();
    num1(num2);
    num1(fnum());

    num12[1] = 1;
    fmostrar(num1, num2);
    if ((j < 5) && (k > 10)) {
        fsuma();
        fsuma(1);
        fsuma(1,2);
        fsuma(1,2,3);
        return 1;
    }       
    else{
    while ((i < 5) && (j > 0) || (k != 0)) {
        fmostrar(num1, num2);
    }
        return 0;
    }
}

void fhola(int algo){
    int a;
}
